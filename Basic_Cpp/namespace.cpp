#include <iostream>
 
int main()
{
   using namespace std; // this using directive tells the compiler that we're using everything in the std namespace!
   cout << "Hello world!" << endl; // so no std:: prefix is needed here!
   return 0;
}